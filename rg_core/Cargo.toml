[package]
name = "rg_core"
version = "0.1.0"
edition = "2021"

[dependencies]
glam = { version = "0.29", features = ["bytemuck"] }
log = "0.4"
rand = "0.8"
bytemuck = { version = "1.19", features = ["derive"] }
